CREATE OR REPLACE PROCEDURE "DELETE_FUNCIONARIO" (vCOD_FUNCIONARIO number,
 vNUM      INT(10)NOT NULL,
  vOPR        CHAR)
IS
  vEXCEPTION EXCEPTION;
BEGIN
  IF (vOPR = 'D') THEN
    DELETE FROM FUNCIONARIO WHERE N_FUNCIONARIO = vCOD_FUNCIONARIO;
ELSE
    RAISE vEXCEPTION;
  END IF;
  EXCEPTION
    WHEN vEXCEPTION THEN
      RAISE_APPLICATION_ERROR(-20999,'ATENÇÃO! Operação diferente de D.', FALSE);
END DELETE_FUNCIONARIO;