CREATE OR REPLACE PROCEDURE "UPDATE_N_GERENTE" (vCOD_FUNCIONARIO number,
 vNUM      INT(10),
  vOPR        CHAR)
IS
  vEXCEPTION EXCEPTION;
BEGIN
  IF (vOPR = 'U') THEN
    UPDATE FUNCIONARIO SET n_pessoa = vNUM WHERE N_GERENTE = vCOD_FUNCIONARIO;
ELSE
    RAISE vEXCEPTION;
  END IF;
  EXCEPTION
    WHEN vEXCEPTION THEN
      RAISE_APPLICATION_ERROR(-20999,'ATENÇÃO! Operação diferente de U.', FALSE);
END UPDATE_N_GERENTE;