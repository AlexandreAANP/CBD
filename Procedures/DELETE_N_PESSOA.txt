CREATE OR REPLACE PROCEDURE "DELETE_N_PESSOA" (vCOD_FUNCIONARIO number,
 vNUM      INT(20)NOT NULL,
  vOPR        CHAR)
IS
  vEXCEPTION EXCEPTION;
BEGIN
  IF (vOPR = 'D') THEN
    DELETE FROM FUNCIONARIO WHERE N_PESSOA = vCOD_FUNCIONARIO;
ELSE
    RAISE vEXCEPTION;
  END IF;
  EXCEPTION
    WHEN vEXCEPTION THEN
      RAISE_APPLICATION_ERROR(-20999,'ATENÇÃO! Operação diferente de D.', FALSE);
END DELETE_N_PESSOA;