CREATE OR REPLACE PROCEDURE "INSERT_N_PESSOA" (vCOD_FUNCIONARIO number,
vNUM      INT(20)NOT NULL,
  vOPR        CHAR)
IS
  vEXCEPTION EXCEPTION;
BEGIN
  IF (vOPR = 'I') THEN
    INSERT INTO FUNCIONARIO( n_pessoa) VALUES (vNUM);
ELSE
    RAISE vEXCEPTION;
  END IF;
  EXCEPTION
    WHEN vEXCEPTION THEN
      RAISE_APPLICATION_ERROR(-20999,'ATENÇÃO! Operação diferente de I.', FALSE);
END INSERT_N_PESSOA;